<div class="jumbotron">   
	<h1>Minhas Tarefas</h1>
</div>
<div>
		<% if @tasks.present? %>
			<table class="table">
				<tr>
			    <th>Tarefa</th>
			    <th>Status</th>
			    <th>Ação destrutiva!</th>
			  </tr>
			<% @tasks.each do |task| %>
			  <tr>  
					<td><%= task.title %></td>
					<td><%= 'Finalizada' if task.status.present?  %></td>
					<td><%= link_to 'Remover', task_path(task),
                    method: :delete, data: { confirm: 'Quer mesmo deletar essa tarefinha?' }, :class => "btn btn-default btn-xs" %>
              <%= link_to 'Editar', task_path(task), :class => "btn btn-default btn-xs" %>
              <%= link_to 'Feito!', task_path(task), method: :patch, :class => "btn btn-default btn-xs" %></td>          
				</tr>
			<% end %>	
			</table>
		<% else %>
			<p>Adicione novas tarefas!</p>
		<% end %>	
		<%= link_to 'Adicionar tarefa', new_task_path, :class => "btn btn-default" %>  
</div>